# NGX Layout — Cursor Agent Skill

**Scope:** Spacing, 8px grid, type hierarchy, Z1–Z4 zones, color economy, density modes, Gantt layout, alignment contracts.

**Invoke:** `/ngx-layout` or rely on the skill `description` in frontmatter when editing layout-heavy NGX UI.

**Pair with:** [ngx-design-system](../ngx-design-system/) for broader patterns, compliance, and construction-domain UI rules.

**Source:** `SKILL.md` in this folder (full prescriptive rules).
